import { jacquesFrancois } from "@/utils/font";

const ConstructText = ()=>{
    return (
        <div className="pt-6 pb-2 flex justify-center">
            <h1 className={jacquesFrancois.className + " inline-block text-2xl"}>Comming Soon...</h1>
        </div>
    );
}

export default ConstructText;